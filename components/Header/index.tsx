import headerStyle from './header.module.scss';

const Header = () => {
    return (
        <div className={headerStyle.header}>
            <header className={headerStyle['nav-wrapper']}>fack pilo</header>
        </div>
    );
};

export default Header;
