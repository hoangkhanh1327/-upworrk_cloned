import SearchBox from './SearchBox';

const Action = () => {
    return (
        <div className='ml-auto flex items-center'>
            <SearchBox />
        </div>
    );
};

export default Action;
