const ClientDashboardLayout = ({ children }: { children: React.ReactNode }) => {
    return (
        <>
            <div></div>
            {children}
        </>
    );
};

export default ClientDashboardLayout;
